import EventsClient from '@/components/EventsClient';

export const metadata = {
    title: "Our Works | eventsclick",
    description: "Browse our portfolio of corporate events, brand activations, and creative projects."
};

export default function EventsPage() {
    return <EventsClient />;
}
